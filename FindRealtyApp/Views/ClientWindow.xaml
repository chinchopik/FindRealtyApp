<Window x:Class="FindRealtyApp.Views.ClientWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FindRealtyApp.Views"
        mc:Ignorable="d"
        Title="ClientWindow" Height="450" Width="800">
    <Grid ShowGridLines="True">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
        </Grid.RowDefinitions>

        <Border>
            <Border.Background>
                <ImageBrush ImageSource="/Resources/loginwallpaper.jpg"/>
            </Border.Background>
        </Border>

        <StackPanel>
            <StackPanel HorizontalAlignment="Center">
                <Rectangle Fill="Gray" Width="100"
                        Height="75" Margin="10"/>
                <TextBlock Text="EntireWorld" HorizontalAlignment="Center" FontSize="18"/>
            </StackPanel>
            <StackPanel Margin="50"
                       >
                <Button Content="lol"/>
                <Button Content="lol1"/>
                <Button Content="lol2"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
